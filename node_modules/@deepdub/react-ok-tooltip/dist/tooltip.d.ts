export declare const DEFAULT_DELAY = 1000;
export interface TooltipProps {
    title: string;
    subtitle?: string;
    maxWidth?: string;
}
export declare function tooltip(title: string, { groupId, ...appTooltipProps }?: {
    groupId?: string | null;
} & Omit<TooltipProps, 'title'>): (element: HTMLElement | null) => void;
